#include<stdio.h>
struct student{
    char name[50];
    char branch[50];
    int age;
    double cgpa;
};
int main()
{
void inputStudent(struct student student *s);
printf("Enter Name");
scanf("%[^\n]",s->name);
printf("Enter Branch ");
scanf("%s",s->branch);
printf("Enter Age ");
scanf("%d",&s->age);
printf("Enter CGPA");
scanf("%lf",&s->cgpa);
}
int main()
{
    struct student students[60]
    int numStudents;
    for(i=0;i<=numStudents;++i){
        printf("\n students %d \n",i+1)
        inputStudent(&students[i])
    };
    printf("details of all students\n");
    for(i=0;i<=numStudents;++i)
    {
        printf("%d \n",i+1);
       printf("%s\n %s \n %d \n %f",student1.name,student1.branch,student1.age,student1.cgpa);
return 0;
    }
    
}
